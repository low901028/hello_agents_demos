### 说明
本仓库主要记录一些与agent 应用开发相关的demo，主要是以rust为主

### 注意
本仓库中使用的相关涉及api key关键的信息都放到，当前项目根目录下的.env文件中；
LLM以deepseek为主，搜索引擎api以百度千帆为主[按需配置调整对应的代码即可]
格式如下：
```python
key(见代码中使用)=value(对应的开发者平台提供的api key)
```